-- ReadSphere Database Initialization Script
-- This file will be executed when the PostgreSQL container is first created

-- For now, we'll let Hibernate/JPA handle schema creation via ddl-auto=update
-- Tables will be created automatically based on entity annotations

-- Optional: You can add seed data or custom indexes here

-- Example seed data can be added after Hibernate creates the tables
-- This script runs only once on container initialization
